{
	"name": "litellm-vscode-chat",
	"displayName": "LiteLLM Provider for GitHub Copilot Chat",
	"description": "An extension that integrates LiteLLM into GitHub Copilot Chat",
	"version": "0.3.1",
	"publisher": "vivswan",
	"license": "MIT",
	"icon": "assets/logo.png",
	"galleryBanner": {
		"color": "#100f11",
		"theme": "dark"
	},
	"badges": [
		{
			"url": "https://img.shields.io/github/stars/Vivswan/litellm-vscode-chat?style=social",
			"description": "Star litellm-vscode-chat on Github",
			"href": "https://github.com/Vivswan/litellm-vscode-chat"
		}
	],
	"homepage": "https://marketplace.visualstudio.com/items?itemName=vivswan.litellm-vscode-chat",
	"repository": {
		"type": "git",
		"url": "https://github.com/Vivswan/litellm-vscode-chat"
	},
	"bugs": {
		"url": "https://github.com/Vivswan/litellm-vscode-chat/issues"
	},
	"engines": {
		"vscode": "^1.129.0"
	},
	"categories": [
		"AI",
		"Chat"
	],
	"main": "./dist/extension.js",
	"contributes": {
		"languageModelChatProviders": [
			{
				"vendor": "litellm",
				"displayName": "LiteLLM",
				"configuration": {
					"properties": {
						"baseUrl": {
							"type": "string",
							"title": "Base URL",
							"description": "LiteLLM server URL, e.g. http://localhost:4000",
							"format": "uri"
						},
						"apiKey": {
							"type": "string",
							"secret": true,
							"title": "API Key",
							"description": "LiteLLM API key. Leave empty for servers without authentication."
						}
					},
					"required": [
						"baseUrl"
					]
				}
			}
		],
		"commands": [
			{
				"command": "litellm.manage",
				"title": "Manage LiteLLM Provider"
			},
			{
				"command": "litellm.testConnection",
				"title": "LiteLLM: Test Connection"
			},
			{
				"command": "litellm.syncModels",
				"title": "LiteLLM: Sync Models Now"
			},
			{
				"command": "litellm.showDiagnostics",
				"title": "LiteLLM: Show Diagnostics"
			},
			{
				"command": "litellm.helpAndFeedback",
				"title": "LiteLLM: Help & Feedback"
			},
			{
				"command": "litellm.reportIssue",
				"title": "LiteLLM: Report Issue"
			}
		],
		"configuration": {
			"title": "LiteLLM VS Code Chat",
			"properties": {
				"litellm-vscode-chat.defaultMaxOutputTokens": {
					"type": "number",
					"default": 16000,
					"minimum": 1,
					"description": "Default maximum output tokens when not provided by model info"
				},
				"litellm-vscode-chat.defaultContextLength": {
					"type": "number",
					"default": 128000,
					"minimum": 1,
					"description": "Default context length when not provided by model info"
				},
				"litellm-vscode-chat.defaultMaxInputTokens": {
					"type": [
						"number",
						"null"
					],
					"default": null,
					"minimum": 1,
					"description": "Default maximum input tokens (if null, calculated as contextLength - maxOutputTokens)"
				},
				"litellm-vscode-chat.promptCaching.enabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable prompt caching for supported models (currently Anthropic Claude). Caches the system prompt to reduce costs and improve response times. Only affects models that advertise prompt caching support."
				},
				"litellm-vscode-chat.modelParameters": {
					"type": "object",
					"default": {},
					"description": "Model-specific request parameters to customize API behavior. Supports max_tokens, temperature, top_p, frequency_penalty, presence_penalty, stop, etc. Uses longest prefix matching (e.g., 'gpt-4' matches 'gpt-4-turbo:openai'). To scope parameters to a specific server, prefix with the server's base URL (e.g., 'http://localhost:4000/gpt-4') or, for servers configured before the provider-group migration, the server label (e.g., 'Production/gpt-4'). Server-scoped entries take priority over unscoped ones. Only parameters you set are sent; the extension never injects defaults.",
					"additionalProperties": {
						"type": "object"
					}
				},
				"litellm-vscode-chat.headers": {
					"type": "object",
					"default": {},
					"description": "Custom HTTP headers to include with all LiteLLM requests (model discovery and chat completions). Useful for API gateways that require headers like x-litellm-api-key. If values contain secrets, prefer User settings (not workspace settings) to avoid accidental commits.",
					"additionalProperties": {
						"type": [
							"string",
							"number",
							"boolean"
						]
					}
				},
				"litellm-vscode-chat.maskApiKeyInput": {
					"type": "boolean",
					"default": true,
					"description": "Mask the API key input field when configuring. Disable to see the key as you type."
				},
				"litellm-vscode-chat.requestTimeout": {
					"type": "number",
					"default": 300000,
					"minimum": 1000,
					"description": "Request timeout in milliseconds for chat completion requests. Default is 300000ms (5 minutes). Increase this value if you're experiencing timeout errors with long-running requests."
				},
				"litellm-vscode-chat.discoveryTimeout": {
					"type": "number",
					"default": 30000,
					"minimum": 1000,
					"description": "Request timeout in milliseconds for model discovery requests. Default is 30000ms (30 seconds)."
				},
				"litellm-vscode-chat.discoveryCacheTtl": {
					"type": "number",
					"default": 3600000,
					"minimum": 0,
					"description": "How long, in milliseconds, discovered model lists are reused before the server is asked again. Default is 3600000 (1 hour). Higher values avoid repeated discovery requests when VS Code re-resolves providers in bursts, but models added or removed on the server take up to this long to appear; lower values pick up server-side changes sooner at the cost of more requests. Set to 0 to ask the server on every refresh (simultaneous refreshes still share one request). Run 'LiteLLM: Sync Models Now' to refresh immediately regardless of this setting."
				}
			}
		}
	},
	"scripts": {
		"prepare": "husky",
		"vscode:prepublish": "tsc -p tsconfig.prod.json && bun run bundle",
		"compile": "tsc -p ./",
		"bundle": "bun scripts/esbuild.mts --production && bun scripts/generate-third-party-notices.ts",
		"bundle:dev": "bun scripts/esbuild.mts",
		"bundle:watch": "bun scripts/esbuild.mts --watch",
		"typecheck": "tsc --noEmit -p ./ && tsc --noEmit -p tsconfig.scripts.json",
		"setup-env": "bash scripts/setup-env.sh",
		"setup-env:verify": "bash scripts/setup-env.sh --verify",
		"setup-env:full": "bash scripts/setup-env.sh --full",
		"setup-env:pwsh": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/setup-env.ps1",
		"setup-env:verify:pwsh": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/setup-env.ps1 -Verify",
		"setup-env:full:pwsh": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/setup-env.ps1 -Full",
		"lint:actions": "bun scripts/lint-actions.ts",
		"lint:knip": "knip",
		"lint:types": "eslint src scripts",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"watch": "tsc -watch -p ./",
		"docker:up": "bun scripts/compose.ts up -d --wait",
		"docker:down": "bun scripts/compose.ts down -v",
		"docker:logs": "bun scripts/compose.ts logs -f",
		"generate-config": "bun scripts/generate-litellm-config.ts",
		"test": "bun run compile && bun run bundle:dev && vscode-test --label unit",
		"test:coverage": "bun run compile && bun run bundle:dev && vscode-test --label unit --coverage && bun scripts/check-coverage.ts",
		"test:docker": "bun scripts/docker-test.ts",
		"host-fidelity-test": "bun scripts/host-fidelity-test.ts"
	},
	"dependencies": {
		"openai": "^6.48.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.4",
		"@types/mocha": "^10.0.10",
		"@types/node": "^26.1.1",
		"@types/vscode": "^1.125.0",
		"@vscode/test-cli": "^0.0.15",
		"@vscode/test-electron": "^3.0.0",
		"@vscode/vsce": "^3.9.2",
		"actionlint": "^2.0.6",
		"esbuild": "^0.28.1",
		"eslint": "^10.7.0",
		"fast-check": "^4.9.0",
		"husky": "^9.1.7",
		"jiti": "^2.7.0",
		"knip": "^6.27.0",
		"lint-staged": "^17.0.8",
		"msw": "^2.15.0",
		"typescript": "^6.0.3",
		"typescript-eslint": "8.64.1-alpha.10"
	},
	"lint-staged": {
		"*.{js,mjs,cjs,ts,jsx,tsx}": "biome format --write --no-errors-on-unmatched"
	},
	"packageManager": "bun@1.3.9"
}
