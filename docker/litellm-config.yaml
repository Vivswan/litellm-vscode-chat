# Output of scripts/generate-litellm-config.ts; do not edit the fake
# model entries by hand. After changing src/test/scenarios.ts, run:
#   bun run generate-config

model_list:
  # Fake scenario models served by the fake-openai container. LiteLLM
  # strips the openai/ prefix, so the backend receives the bare scenario
  # name as the model ID.
  - model_name: fake/text-only
    litellm_params:
      model: openai/text-only
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/load-balanced
    litellm_params:
      model: openai/load-balanced
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/load-balanced
    litellm_params:
      model: openai/load-balanced-b
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 64000
      max_output_tokens: 8000
      max_tokens: 8000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/long-text
    litellm_params:
      model: openai/long-text
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/structured-content
    litellm_params:
      model: openai/structured-content
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/reasoning
    litellm_params:
      model: openai/reasoning
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_reasoning: true

  - model_name: fake/tool-call-single
    litellm_params:
      model: openai/tool-call-single
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/tool-call-chunked
    litellm_params:
      model: openai/tool-call-chunked
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/usage-only-final
    litellm_params:
      model: openai/usage-only-final
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/error-400
    litellm_params:
      model: openai/error-400
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000

  - model_name: fake/error-401
    litellm_params:
      model: openai/error-401
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000

  - model_name: fake/error-429
    litellm_params:
      model: openai/error-429
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000

  - model_name: fake/slow-stream
    litellm_params:
      model: openai/slow-stream
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/thinking-structured
    litellm_params:
      model: openai/thinking-structured
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_reasoning: true

  - model_name: fake/text-then-tool
    litellm_params:
      model: openai/text-then-tool
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/parallel-tool-calls
    litellm_params:
      model: openai/parallel-tool-calls
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/usage-with-cache-tokens
    litellm_params:
      model: openai/usage-with-cache-tokens
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_prompt_caching: true

  - model_name: fake/reasoning-field
    litellm_params:
      model: openai/reasoning-field
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_reasoning: true

  - model_name: fake/inline-tool-call-tokens
    litellm_params:
      model: openai/inline-tool-call-tokens
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/control-token-sections
    litellm_params:
      model: openai/control-token-sections
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/finish-length
    litellm_params:
      model: openai/finish-length
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/finish-content-filter
    litellm_params:
      model: openai/finish-content-filter
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/tool-call-invalid-json
    litellm_params:
      model: openai/tool-call-invalid-json
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/duplicate-tool-calls
    litellm_params:
      model: openai/duplicate-tool-calls
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true

  - model_name: fake/structured-content-mixed
    litellm_params:
      model: openai/structured-content-mixed
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/refusal
    litellm_params:
      model: openai/refusal
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/annotations
    litellm_params:
      model: openai/annotations
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/thinking-blocks
    litellm_params:
      model: openai/thinking-blocks
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_reasoning: true

  - model_name: fake/redacted-thinking
    litellm_params:
      model: openai/redacted-thinking
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_reasoning: true

  - model_name: fake/audio-output
    litellm_params:
      model: openai/audio-output
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true

  - model_name: fake/usage-token-details
    litellm_params:
      model: openai/usage-token-details
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_prompt_caching: true

  - model_name: fake/dynamic
    litellm_params:
      model: openai/dynamic
      api_base: http://fake-openai:8080/v1
      api_key: fake-key
      # Forward params LiteLLM would otherwise reject for plain openai
      # models; the extension's pass-through contract is under test.
      allowed_openai_params: ["reasoning_effort", "verbosity"]
    model_info:
      max_input_tokens: 128000
      max_output_tokens: 16000
      max_tokens: 16000
      supports_function_calling: true
      supports_tool_choice: true
      supports_vision: true
      supports_pdf_input: true
      supports_reasoning: true

  # Optional real providers; keys arrive via .env through docker-compose.
  # Without a key the entry stays registered and fails only when called.
  - model_name: openai/*
    litellm_params:
      model: openai/*
      api_key: os.environ/OPENAI_API_KEY
  - model_name: anthropic/*
    litellm_params:
      model: anthropic/*
      api_key: os.environ/ANTHROPIC_API_KEY
  - model_name: github/*
    litellm_params:
      model: github/*
      api_key: os.environ/GITHUB_API_KEY
  # Passthrough for any other provider/model LiteLLM can infer.
  - model_name: "*"
    litellm_params:
      model: "*"

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
