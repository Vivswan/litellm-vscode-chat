## Pick a LiteLLM model

Open the model picker at the bottom of the chat input. Models from your LiteLLM server appear under the server's name; pick one and chat with it like any built-in model.

If the list looks stale after you change models on the server, run "LiteLLM: Sync Models Now" from the Command Palette.
