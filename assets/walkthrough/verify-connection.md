## Verify the connection

The connection test contacts every configured server, refreshes its model list, and reports the result.

The LiteLLM status bar item (bottom right) shows the same state at a glance; click it for per-server diagnostics.
