## Verify the connection

The connection test contacts every configured server, refreshes its model list, and reports the result.

The LiteLLM status bar item (bottom right) shows the same state at a glance; click it to open the dashboard, whose Diagnostics tab has the per-server detail.
